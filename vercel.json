{
  "version": 2,
  "buildCommand": "echo \"Skipping frontend build (API only)\"",
  "builds": [
    { "src": "api/*.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1.js" }
  ]
}
